# Dynamic login background
This extension is used to set a dynamic background image for the TYPO3 login screen. The image is selected randomly from a specified folder.

## How to install this extension

```bash
composer require strukteon/dynamic-login-background
```

## How to use this extension
In the TYPO3 extension configuration, you can configure the folder.